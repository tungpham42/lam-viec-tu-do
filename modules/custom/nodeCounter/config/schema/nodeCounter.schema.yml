# Schema for the configuration files of the nodeCounter module.

nodeCounter.settings:
  type: config_object
  label: 'Node Counter settings'
  mapping:
    count_content_views:
      type: integer
      label: 'Count content views'
    display_max_age:
      type: integer
      label: 'How long any nodeCounter may be cached, i.e. the refresh interval'

block.settings.nodeCounter_popular_block:
  type: block_settings
  label: 'Popular content block settings'
  mapping:
    top_day_num:
      type: integer
      label: 'Number of day\s top views to display'
    top_all_num:
      type: integer
      label: 'Number of all time views to display'
    top_last_num:
      type: integer
      label: 'Number of most recent views to display'
